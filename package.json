{
  "name": "Indigov Challenge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --raw --kill-others-on-fail \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "npm --prefix ./api run dev",
    "dev:web": "npm --prefix ./web run dev"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "concurrently": "^9.0.1"
  }
}